import { Text, View } from "react-native";
import HomePage from "@/components/home-page";

const Index = () => {
  return (
    <View style={{flex: 1}}>
      <HomePage />
    </View>
  );
}

export default Index